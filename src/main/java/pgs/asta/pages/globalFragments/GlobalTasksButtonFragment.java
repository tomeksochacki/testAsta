package pgs.asta.pages.globalFragments;

import org.openqa.selenium.WebDriver;
import pgs.asta.pages.PageObjectBase;

public class GlobalTasksButtonFragment extends PageObjectBase {

    public GlobalTasksButtonFragment(WebDriver driver) {
        super(driver);
    }
}
